import React from "react";
import ToDoList from "@components/ToDoList";
import Layout from "layout";

const Home = () => {
    return (
        <Layout>
            <ToDoList />
        </Layout>
    );
};

export default Home;
